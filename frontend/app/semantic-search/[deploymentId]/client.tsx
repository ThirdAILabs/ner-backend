'use client';

export default function SemanticSearchClient({ deploymentId }: { deploymentId: string }) {
  return (
    <div>
      <h1>Semantic Search - {deploymentId}</h1>
      <p>This is a placeholder for the semantic search functionality.</p>
    </div>
  );
} 